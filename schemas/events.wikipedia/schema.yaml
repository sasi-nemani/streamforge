# StreamForge Schema — events.wikipedia
# Version: 1.0.0
# Inferred: 2026-03-19T16:05:09.595843+00:00
# Events sampled: 150
# Overall confidence: 60%
#
# This file is the source of truth for stream: events.wikipedia
# Edit this file to declare corrections. Run 'streamforge watch' to detect future drift.

stream: events.wikipedia
version: 1.0.0
inferred_at: '2026-03-19T16:05:09.595843+00:00'
inference_confidence: 0.6
inference_model: llama-3.3-70b-versatile
event_count_sampled: 150
event_types:
- edit
- categorize
- log
- new
fields:
- path: meta
  type: object
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: 'Nested object — contains: domain, dt, id, offset, partition, request_id'
- path: meta.uri
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  pii:
  - passport
  notes: Statistically inferred (LLM fallback)
- path: meta.request_id
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: meta.id
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: meta.domain
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: meta.stream
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: meta.dt
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: meta.topic
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: meta.partition
  type: integer
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: meta.offset
  type: integer
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: id
  type: integer
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: type
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: namespace
  type: integer
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: title
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  pii:
  - passport
  notes: Statistically inferred (LLM fallback)
- path: title_url
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  pii:
  - passport
  notes: Statistically inferred (LLM fallback)
- path: comment
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: timestamp
  type: integer
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: user
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: bot
  type: boolean
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: notify_url
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: minor
  type: boolean
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: patrolled
  type: boolean
  required: false
  nullable: false
  presence_rate: 0.7671
  confidence: 0.6534
  notes: Statistically inferred (LLM fallback)
- path: length
  type: object
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: 'Nested object — contains: new, old'
- path: length.old
  type: integer
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: length.new
  type: integer
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: revision
  type: object
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: 'Nested object — contains: new, old'
- path: revision.old
  type: integer
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: revision.new
  type: integer
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: server_url
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: server_name
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: server_script_path
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: wiki
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: parsedcomment
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
