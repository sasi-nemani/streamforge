// Generated by StreamForge
// Source: events.payments  Schema: v1.0.0
// Fields: 9  Confidence: 60%
// Inferred: 2026-03-19

syntax = "proto3";

package io.streamforge.events;

message EventsPayments {
    double amount = 1;  // Statistically inferred (LLM fallback)
    string currency = 2;  // Statistically inferred (LLM fallback)
    string event_id = 3;  // Statistically inferred (LLM fallback)
    string event_type = 4;  // Statistically inferred (LLM fallback)
    string merchant = 5;  // Statistically inferred (LLM fallback)
    string status = 6;  // Statistically inferred (LLM fallback)
    int64 timestamp = 7;  // Statistically inferred (LLM fallback)
    string user_id = 8;  // Statistically inferred (LLM fallback)
    string user_email = 9;  // optional | PII:email | Statistically inferred (LLM fallback)
}