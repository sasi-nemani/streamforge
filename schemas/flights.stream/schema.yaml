# StreamForge Schema — flights.stream
# Version: 1.0.0
# Inferred: 2026-03-14T11:17:09.326987+00:00
# Events sampled: 400
# Overall confidence: 92%
#
# This file is the source of truth for stream: flights.stream
# Edit this file to declare corrections. Run 'streamforge watch' to detect future drift.

stream: flights.stream
version: 1.0.0
inferred_at: '2026-03-14T11:17:09.326987+00:00'
inference_confidence: 0.92
inference_model: llama-3.3-70b-versatile
event_count_sampled: 400
event_types:
- flight.gate_changed
- flight.landed
- flight.delayed
- flight.status_changed
- flight.departed
fields:
- path: event_id
  type: uuid
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 1.0
  notes: Unique identifier for the event, in standard UUID v4 format
- path: event_type
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 1.0
  enum_values:
  - flight.gate_changed
  notes: Type of event, e.g. 'flight.gate_changed'
- path: flight_number
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 1.0
  notes: Flight number, e.g. 'EK6158'
- path: origin
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 1.0
  notes: Origin airport code, e.g. 'MAN'
- path: destination
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 1.0
  notes: Destination airport code, e.g. 'CDG'
- path: scheduled_departure
  type: timestamp_iso8601
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 1.0
  notes: Scheduled departure time in ISO 8601 format
- path: actual_departure
  type: timestamp_iso8601
  required: false
  nullable: true
  presence_rate: 1.0
  confidence: 0.9
  notes: Actual departure time in ISO 8601 format, may be null if not available
- path: delay_minutes
  type: integer
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 1.0
  notes: Delay in minutes, e.g. 0 or 112
- path: status
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 1.0
  enum_values:
  - BOARDING
  - ON_TIME
  - CANCELLED
  - DELAYED
  - DEPARTED
  notes: Flight status, e.g. 'BOARDING', 'ON_TIME', 'CANCELLED', 'DELAYED'
- path: gate
  type: string
  required: false
  nullable: true
  presence_rate: 1.0
  confidence: 0.9
  notes: Gate number, e.g. 'A22', may be null if not available
- path: aircraft_type
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 1.0
  notes: Aircraft type, e.g. 'A320', 'B777', 'A380'
- path: passenger_count
  type: integer
  required: false
  nullable: true
  presence_rate: 1.0
  confidence: 0.9
  notes: Number of passengers, may be null if not available
- path: timestamp
  type: mixed
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.95
  notes: Timestamp, either in Unix epoch milliseconds or ISO 8601 format
- path: severity
  type: string
  required: false
  nullable: true
  presence_rate: 0.3441
  confidence: 0.5
  enum_values:
  - INFO
  - CRITICAL
  - WARNING
  notes: Severity level, e.g. 'INFO', 'CRITICAL', 'WARNING', may be null if not available
- path: crew_captain
  type: string
  required: false
  nullable: true
  presence_rate: 0.1613
  confidence: 0.2
  notes: Crew captain name, may be null if not available
