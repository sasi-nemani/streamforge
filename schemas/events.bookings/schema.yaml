# StreamForge Schema — events.bookings
# Version: 1.0.0
# Inferred: 2026-03-19T16:04:50.267794+00:00
# Events sampled: 150
# Overall confidence: 60%
#
# This file is the source of truth for stream: events.bookings
# Edit this file to declare corrections. Run 'streamforge watch' to detect future drift.

stream: events.bookings
version: 1.0.0
inferred_at: '2026-03-19T16:04:50.267794+00:00'
inference_confidence: 0.6
inference_model: llama-3.3-70b-versatile
event_count_sampled: 150
event_types:
- booking.created
- booking.cancelled
- booking.updated
fields:
- path: booking_ref
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: event_type
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: airline
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: origin
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: destination
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: cabin
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: status
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: total_price
  type: float
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: currency
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: created_at
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: passengers
  type: array
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  notes: Statistically inferred (LLM fallback)
- path: passengers[].passenger_name
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  pii:
  - name
  notes: Statistically inferred (LLM fallback)
- path: passengers[].ticket_number
  type: string
  required: true
  nullable: false
  presence_rate: 1.0
  confidence: 0.7
  pii:
  - passport
  notes: Statistically inferred (LLM fallback)
- path: passengers[].passport_number
  type: string
  required: false
  nullable: false
  presence_rate: 0.7544
  confidence: 0.6509
  pii:
  - passport
  notes: Statistically inferred (LLM fallback)
- path: contact_email
  type: string
  required: false
  nullable: false
  presence_rate: 0.5263
  confidence: 0.6053
  pii:
  - email
  notes: Statistically inferred (LLM fallback)
- path: contact_phone
  type: string
  required: false
  nullable: false
  presence_rate: 0.386
  confidence: 0.5772
  pii:
  - phone
  notes: Statistically inferred (LLM fallback)
- path: seat_preference
  type: string
  required: false
  nullable: false
  presence_rate: 0.1754
  confidence: 0.5351
  notes: Statistically inferred (LLM fallback)
- path: passengers[].frequent_flyer_number
  type: string
  required: false
  nullable: false
  presence_rate: 0.5263
  confidence: 0.6053
  pii:
  - loyalty_number
  notes: Statistically inferred (LLM fallback)
- path: baggage_allowance_kg
  type: integer
  required: false
  nullable: false
  presence_rate: 0.2982
  confidence: 0.5596
  notes: Statistically inferred (LLM fallback)
- path: passengers[].date_of_birth
  type: string
  required: false
  nullable: false
  presence_rate: 0.4211
  confidence: 0.5842
  pii:
  - phone
  - date_of_birth
  notes: Statistically inferred (LLM fallback)
- path: special_meal
  type: string
  required: false
  nullable: false
  presence_rate: 0.2105
  confidence: 0.5421
  notes: Statistically inferred (LLM fallback)
